buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2'  // 降低到兼容的版本
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
